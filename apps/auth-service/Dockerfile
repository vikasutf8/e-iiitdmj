# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @e-shop/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t e-shop-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system e-shop-auth-service && \
          adduser --system -G e-shop-auth-service e-shop-auth-service

COPY apps/auth-service/dist e-shop-auth-service/
COPY apps/auth-service/package.json e-shop-auth-service/
RUN chown -R e-shop-auth-service:e-shop-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix e-shop-auth-service --omit=dev -f install

CMD [ "node", "e-shop-auth-service" ]
